# 建账规则助手系统 - 环境变量配置示例
# 复制此文件为 .env 并填写实际值

# ==========================================
# SiliconFlow API 配置
# ==========================================
# 获取 API Key: https://cloud.siliconflow.cn/
SILICONFLOW_API_KEY=your_api_key_here
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# ==========================================
# 数据库配置（PostgreSQL）
# ==========================================
# 注意：需要安装 PGVector 扩展
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vector_db
DB_USER=postgres
DB_PASSWORD=your_password_here
PGDATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/vector_db

# ==========================================
# 对象存储配置（可选）
# ==========================================
# 如果使用本地存储，可以留空
# 如果使用 S3 兼容存储，需要配置相应参数
STORAGE_TYPE=local
STORAGE_PATH=d:/AIRcommend/JinRongAgents/agents_final_tech/assets
BUCKET_ENDPOINT_URL=
BUCKET_NAME=

# ==========================================
# Flask Web 服务配置
# ==========================================
FLASK_APP=src/web/app.py
FLASK_ENV=development
FLASK_DEBUG=false
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# ==========================================
# 其他配置
# ==========================================
# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# 临时文件路径
TEMP_DIR=/tmp

# BM25 缓存目录
BM25_CACHE_DIR=/tmp/bm25_cache

# ==========================================
# 使用说明
# ==========================================
# 1. 复制此文件: cp .env.example .env
# 2. 编辑 .env 文件，填写实际配置
# 3. 确保数据库已创建并安装了 PGVector 扩展
# 4. 启动服务: python src/main.py
